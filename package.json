{
  "name": "maana-q-assistant-template",
  "version": "0.1.0",
  "homepage": ".",
  "private": true,
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "watch": "NODE_ENV=${NODE_ENV:=development} webpack --config scripts/devConfig.js --watch --progress",
    "watch:prod": "NODE_ENV=production npm run watch"
  },
  "devDependencies": {
    "webpack-cli": "^3.3.11"
  },
  "dependencies": {
    "@io-maana/q-assistant-client": "^3.2.1-beta.14",
    "dotenv": "^8.2.0",
    "react": "^16.12.0",
    "react-dom": "^16.12.0",
    "react-scripts": "^3.4.0",
    "serve": "^11.3.0"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      "last 3 chrome version",
      "last 3 edge version"
    ],
    "development": [
      "last 1 chrome version"
    ]
  }
}
